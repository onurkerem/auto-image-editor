# auto-image-editor website

Marketing website for the auto-image-editor CLI tool.

## Prerequisites

- Node.js >= 18

## Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The static output is in `dist/`.
